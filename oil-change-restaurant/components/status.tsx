import {Check,Clock3,Wrench,CircleCheck} from 'lucide-react'; import {cn} from '@/lib/utils';
const steps=[['scheduled','Scheduled',Clock3],['in-progress','In progress',Wrench],['completed','Completed',CircleCheck]] as const;
export function StatusSteps({status}:{status:'scheduled'|'in-progress'|'completed'|'cancelled'}){const active=status==='cancelled'?-1:steps.findIndex(s=>s[0]===status);return <div className='flex items-start'>{steps.map(([key,label,Icon],i)=><div key={key} className='flex flex-1 items-start'><div className='flex flex-col items-center'><div className={cn('grid h-10 w-10 place-items-center rounded-full border-2',i<=active?'border-success bg-emerald-50 text-success':'border-slate-200 bg-white text-slate-400')}>{i<active?<Check size={18}/>:<Icon size={17}/>}</div><span className={cn('mt-2 text-center text-xs font-semibold',i<=active?'text-ink':'text-slate-400')}>{label}</span></div>{i<steps.length-1&&<div className={cn('mt-5 h-0.5 flex-1',i<active?'bg-success':'bg-slate-200')}/>}</div>)}</div>}
